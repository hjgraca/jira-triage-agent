{
  "name": "jira-triage-listener",
  "version": "0.1.0",
  "private": true,
  "description": "Webhook listener that gates Jira Cloud webhooks and spawns one-shot pi triage runs.",
  "main": "listener/server.js",
  "scripts": {
    "start": "node listener/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
