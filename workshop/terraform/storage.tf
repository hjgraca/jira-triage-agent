##############################################
# IRSA role for the EBS CSI driver
##############################################
module "ebs_csi_irsa" {
  source  = "terraform-aws-modules/iam/aws//modules/iam-role-for-service-accounts-eks"
  version = "~> 5.39"

  role_name             = "${var.name}-ebs-csi"
  attach_ebs_csi_policy = true

  oidc_providers = {
    main = {
      provider_arn               = module.eks.oidc_provider_arn
      namespace_service_accounts = ["kube-system:ebs-csi-controller-sa"]
    }
  }

  tags = var.tags
}

##############################################
# Default StorageClass (gp3, WaitForFirstConsumer)
##############################################
# WaitForFirstConsumer delays volume creation until the pod is scheduled, so
# the EBS volume lands in the same AZ as the pod. Without this, multi-AZ node
# groups produce volumes the pod cannot mount.
resource "kubernetes_storage_class_v1" "gp3" {
  metadata {
    name = "gp3"
    annotations = {
      "storageclass.kubernetes.io/is-default-class" = "true"
    }
  }

  storage_provisioner    = "ebs.csi.aws.com"
  volume_binding_mode    = "WaitForFirstConsumer"
  allow_volume_expansion = true
  reclaim_policy         = "Delete"

  parameters = {
    type = "gp3"
  }

  depends_on = [module.eks]
}
