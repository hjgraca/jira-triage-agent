.claude/settings.local.json

# Kube
kubeconfig

# Real triage secrets + config (only the .example templates are committed)
agent/deploy/k8s/base/secrets.yaml
agent/deploy/k8s/base/config.yaml

# macOS junk
.DS_Store

# Delivery artifacts (git archive output) — never commit, any prefix
*-triage-agent-*.tar.gz
*.tar.gz
