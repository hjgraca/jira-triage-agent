.claude/settings.local.json

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
tfplan
*.tfplan
crash.log
*.tfvars
!example.tfvars
.terraform.lock.hcl

# Kube
kubeconfig

# Real triage secrets + config (only the .example templates are committed)
agent/k8s/triage-secrets.yaml
agent/k8s/triage-config.yaml

# macOS junk
.DS_Store

# Local HLD working doc (author-managed, kept out of git)
docs/hld/

# Terraform state — never commit. The customer applies agent/terraform in their
# own account/backend; the workshop module holds the lab's live state locally.
workshop/terraform/*.tfstate
workshop/terraform/*.tfstate.*
agent/terraform/.terraform/
agent/terraform/*.tfstate
agent/terraform/*.tfstate.*
agent/terraform/terraform.tfvars
