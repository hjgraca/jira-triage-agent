.claude/settings.local.json

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
tfplan
*.tfplan
crash.log
*.tfvars
!example.tfvars
.terraform.lock.hcl

# Kube
kubeconfig

# Real triage secrets + config (only the .example templates are committed)
k8s/triage-secrets.yaml
k8s/triage-config.yaml

# macOS junk
.DS_Store

# Local HLD working doc (author-managed, kept out of git)
docs/hld/
